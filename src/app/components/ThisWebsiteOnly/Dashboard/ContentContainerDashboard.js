import React from 'react'

const ContentContainerDashboard = ({ children }) => {
  return (
    <div className={'lg:mt-5 lg:mx-5 bg-white mt-[65px] md:mt-[unset]'}>
      {children}
    </div>
  )
}

export default ContentContainerDashboard
