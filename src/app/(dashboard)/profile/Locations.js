import React from 'react'
import SectionContainer from './SectionContainer'

const Locations = () => {
  // Remove Google Billing
  // Remove Google Billing
  // Remove Google Billing
  return (
    <SectionContainer data={{ title: 'Locations' }}>
      <p className={''}>asdf</p>
    </SectionContainer>
  )
}

export default Locations
